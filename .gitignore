__pycache__
.venv/
log
.vscode
gsl_hit_map

# Python
*.pyc

# Training / experiment outputs
runs/
results/
eval_results/
eval_results_gaden/
obs_viz/
gaden_viz/
gaden_trajectories/
slurm-*.out

# Container images
*.sqsh

# Media
*.png
*.mp4

# Generated caches
geo_cache_*.npz
detect_cache_*.json
# RL checkpoints: stray run outputs stay out of git, but the released
# checkpoints under checkpoints/ are tracked (small, ~6 MB each)
*.pt
!checkpoints/*.pt
# local deploy-time backups of train/deploy-shared files
*.deploy_bak
